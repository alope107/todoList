# Demo TodoList Application

A simple web application that allows users to create Todo lists. It uses express.js and Sequelize. Login occurs via GitHub accounts.

To install:

```npm install ```

To run:

```npm run dev```